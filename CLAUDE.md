# CLAUDE.md

이 저장소에서 작업하는 모든 AI 에이전트(Claude Code 포함)를 위한 지시사항이다.

## 1. 프로젝트 목표

이 프로젝트의 목표와 제약조건, 채점 기준은 **[`CHALLENGE_GOAL.md`](./CHALLENGE_GOAL.md)** 에 정의되어 있다.

**작업을 시작하기 전에 반드시 `CHALLENGE_GOAL.md`를 먼저 읽어라.**

한 줄 요약: *JunctionX Korea 해커톤 과제 — 공공데이터를 활용해 경상북도의 지역 문제를 해결한다. (1) 경상북도 공공데이터를 AI가 즉시 쓸 수 있는 형태(MCP 서버, 오픈소스 skill/plugin 등)로 가공·공개하거나, (2) 공공데이터와 AI로 지역 문제를 발굴·분석해 해결하는 서비스를 만든다.*

## 2. 준수 지시 (MUST)

`CHALLENGE_GOAL.md`의 내용은 이 프로젝트의 **최상위 요구사항**이다. 다음을 준수하라.

- **모든 제안·설계·구현은 `CHALLENGE_GOAL.md`의 필수 준수사항과 채점 기준에 부합해야 한다.** 부합하지 않는 방향은 제안하지 말고, 이미 진행 중이라면 즉시 지적하라.
- **공공데이터를 최소 1개 이상 실제로 사용한다.** 더미/mock 데이터로 대체하고 끝내지 마라. 프로토타이핑 중 임시로 mock을 쓴다면 그 사실과 실제 데이터로 교체할 계획을 명시하라.
- **데이터는 경상북도와 관련된 것이어야 한다.** 무관한 지역 데이터로 범위를 바꾸지 마라.
- **인프라 트랙 산출물은 README만으로 실행 가능해야 한다.** 사전 지식을 요구하는 단계를 남기지 마라. 새 의존성·환경변수·설정을 추가하면 README를 같은 커밋에서 갱신하라.
- **오픈소스 라이선스를 명시하고 유지한다.** `LICENSE` 파일을 지우거나 라이선스와 충돌하는 의존성을 도입하지 마라.
- **경상북도청 명의 저장소로 이관되어 계속 운영될 수 있는 품질**을 기준으로 삼아라. 개인 로컬 환경, 하드코딩된 절대경로, 개인 계정 자격증명에 의존하는 코드를 만들지 마라.
- **API 키·인증정보는 절대 커밋하지 마라.** 공공데이터포털 서비스 키는 환경변수로 처리하고 `.env.example`로 문서화하라.
- **눈으로 확인 가능한 동작하는 프로토타입을 항상 확보하라.** (프로토타입 보너스 10%)

## 3. 의사결정 기준

설계 선택지를 두고 고민할 때는 채점 기준 가중치 순으로 판단하라.

1. 기술적 완성도 (25%) — 실제로 동작하는가
2. 공공부문 활용 가능성 (25%) — 지자체 담당자가 그대로 쓸 수 있는가
3. 혁신성 및 차별성 (20%) — 기존 포털 조회의 재탕이 아닌가
4. 지속가능성 및 임팩트 (20%) — 해커톤 이후에도 유지되는가
5. 프로토타입 보너스 (10%) — 보여줄 수 있는가

## 4. 개발 환경 제약 (MUST)

**개발 환경은 노트북이다.** 디스크 용량, 메모리, 연산 자원이 모두 제한적이라고 전제하라.

- **대용량 모델 가중치를 로컬에 내려받지 마라.** Whisper large, 로컬 TTS 음향 모델, 수 GB짜리 체크포인트 등은 금지다. 필요한 기능은 **API 기반 서비스**로 해결하라.
- **대용량 데이터셋을 통째로 내려받지 마라.** AI Hub 방언 음성 3,000시간 같은 것은 받지 않는다. 필요한 것은 **텍스트 대응쌍 등 경량 자산**뿐이다. 샘플·부분 다운로드로 충분한지 먼저 확인하라.
- **TTS/STT는 클라우드 API를 우선한다.** 한국어 TTS는 [타입캐스트(Typecast)](https://typecast.ai) 를 우선 검토하라. 한국어 음성 품질이 좋고 로컬 자원을 쓰지 않는다. 그 외 후보는 ElevenLabs, Google Cloud TTS.
- **무거운 빌드 도구를 도입하지 마라.** 프론트엔드는 빌드 스텝 없는 순수 HTML/CSS/JS를 유지한다. `node_modules`가 필요한 구조로 만들지 마라.
- **파이썬 의존성은 최소로.** 새 패키지를 추가할 때마다 설치 용량과 시간을 따져라. 표준 라이브러리로 되는 일은 표준 라이브러리로 한다.
- 디스크를 크게 쓰는 작업을 하기 전에 **먼저 알리고 확인을 받아라.**

## 5. 충돌 처리

사용자의 개별 지시와 `CHALLENGE_GOAL.md`가 충돌하는 것으로 보이면, **임의로 판단하지 말고 충돌 지점을 먼저 알리고 확인을 받아라.** 필수 준수사항(탈락 조건)을 위반하게 되는 경우에는 특히 반드시 경고하라.
