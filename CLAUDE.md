# CLAUDE.md

이 저장소에서 작업하는 AI 에이전트(Claude Code 포함)를 위한 지시사항이다.

## 1. 프로젝트

**Voisso (보이소)** — 경북 어르신이 사투리로 전화하면 AI가 알아듣고, 민원을 정리해
경상북도청 담당 부서로 연결한다. 담당자에게는 배정 근거를 함께 전달한다.

프로젝트 설명은 [`README.md`](./README.md) 와 [`ABOUT.md`](./ABOUT.md) 에 있다.
모듈 간 계약(스키마·API·소유권)은 [`docs/CONTRACT.md`](./docs/CONTRACT.md) 가 기준이다.
**작업을 시작하기 전에 계약서를 먼저 읽어라.**

## 2. 데이터 원칙 (MUST)

- **실제 공공데이터를 쓴다.** 더미/mock 으로 대체하고 끝내지 마라. 프로토타이핑 중
  임시로 mock을 쓴다면 그 사실과 실제 데이터로 교체할 계획을 명시하라.
- **데이터는 경상북도와 관련된 것이어야 한다.** 무관한 지역으로 범위를 바꾸지 마라.
- **수집한 데이터를 저장소에 커밋하지 마라.** 이용조건이 기관마다 다르다.
  대신 누구나 직접 생성할 수 있는 수집 스크립트를 제공한다. 근거는 [`docs/DATA_LICENSE.md`](./docs/DATA_LICENSE.md).
- **전화번호를 공개 데이터에 넣지 마라.** `PHONE_0042` 형태 토큰으로 치환하고
  실제 매핑은 gitignore 대상 경로에만 둔다. 담당업무 **본문에 섞인 번호도** 제거한다.
  **가짜 번호를 생성하지 마라.** 실존하는 타인의 번호와 충돌할 수 있다.
- **개인정보는 덜어내는 방향으로만 다룬다.** 신고자 연락처는 자릿수를 보존해 마스킹한다.

## 3. 코드 원칙 (MUST)

- **오픈소스 라이선스를 명시하고 유지한다.** `LICENSE` 를 지우거나 충돌하는 의존성을
  도입하지 마라. 새 의존성의 라이선스를 확인하라.
- **README만 보고 처음 보는 사람이 실행할 수 있어야 한다.** 사전 지식을 요구하는 단계를
  남기지 마라. 새 의존성·환경변수·설정을 추가하면 README를 같은 커밋에서 갱신하라.
- **API 키·인증정보를 절대 커밋하지 마라.** 환경변수로 처리하고 `.env.example` 로 문서화한다.
- **경상북도청 명의 저장소로 이관되어 계속 운영될 수 있는 품질**을 기준으로 삼아라.
  개인 로컬 환경, 하드코딩된 절대경로, 개인 계정 자격증명에 의존하는 코드를 만들지 마라.
- **동작하는 것을 깨뜨리지 마라.** 리팩터링보다 회귀 테스트가 우선이다.

## 4. 개발 환경 제약 (MUST)

**개발 환경은 노트북이다.** 디스크·메모리·연산이 모두 제한적이라고 전제하라.

- **대용량 모델 가중치를 로컬에 내려받지 마라.** Whisper large, 로컬 TTS 음향 모델 등은
  금지다. 필요한 기능은 **API 기반 서비스**로 해결하라.
- **대용량 데이터셋을 통째로 내려받지 마라.** 필요한 것은 경량 자산뿐이다.
  샘플·부분 다운로드로 충분한지 먼저 확인하라.
- **무거운 빌드 도구를 도입하지 마라.** 프론트엔드는 빌드 스텝 없는 순수 HTML/CSS/JS 를
  유지한다. `node_modules` 가 필요한 구조로 만들지 마라.
- **파이썬 의존성은 최소로.** 표준 라이브러리로 되는 일은 표준 라이브러리로 한다.
- **유료 API 를 개발 중에 낭비하지 마라.** 음성 합성·인식은 종량제다.
  테스트는 기본값이 비활성이어야 하고, 명시적 플래그로만 켠다. 계약서 5-D절 참조.
- 디스크를 크게 쓰는 작업 전에 **먼저 알리고 확인을 받아라.**

## 5. 이 서비스가 하지 않는 것

사용자가 고령자이고 상대가 공공기관이므로, 하지 않기로 정한 것들이 있다.

- **행정 처리 결과를 약속하지 않는다.** 접수까지가 이 시스템의 역할이다.
  AI 가 처리 일정이나 가능 여부를 지어내면 그것은 행정 약속이 된다.
- **근거 없는 배정을 하지 않는다.** 확신이 없으면 후보를 여러 개 보여주고
  담당자가 고르게 한다. "모르겠다"고 말할 수 있는 것이 신뢰의 조건이다.
- **응급 상황을 접수로 끝내지 않는다.** 사람이 위험하면 접수보다 119·112 안내가 먼저다.
  접수가 신고를 대체한다고 오해하게 만들면 안 된다.
- **어르신 화면에 기능을 얹지 않는다.** 화면으로 무언가를 하게 만들수록,
  우리가 없애려는 복잡성을 우리가 다시 만든다. 애매하면 빼는 쪽이 옳다.

## 6. 충돌 처리

사용자의 지시와 위 원칙이 충돌하는 것으로 보이면, **임의로 판단하지 말고 충돌 지점을
먼저 알리고 확인을 받아라.** 개인정보·라이선스 관련이면 특히 반드시 경고하라.
