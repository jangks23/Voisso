# Voisso 실행에 필요한 전부다.
#
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# 음성(STT/TTS)은 전용 SDK 없이 표준 라이브러리 urllib 로 REST 호출만 한다.
# 로컬 모델 가중치를 내려받지 않으므로 디스크·메모리 부담이 없다.

fastapi>=0.110
uvicorn[standard]>=0.27

# 대화·요약을 Anthropic 으로 돌릴 때(VOISSO_AGENT_PROVIDER=anthropic)와
# 선택적 방언 다듬기(VOISSO_DIALECT_LLM=1)에서만 쓰인다. OpenAI 경로와 STT/TTS 는
# 표준 라이브러리 urllib 로만 호출한다. 설치돼 있지 않으면 코드가 ImportError 를
# 잡아 그 경로를 건너뛰지만, 한 줄 설치로 선택지를 열어 두는 편이 낫다.
anthropic>=0.40
